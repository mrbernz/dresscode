<p id="notice"><%= notice %></p>

  <% @books.each do |book| %>
    <%= link_to book.title, book %>
    <br>
    <%= book.author %>
    <br>
    <%= book.status %>
    <br>
  <% end %>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
